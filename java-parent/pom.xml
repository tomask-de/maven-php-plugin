<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    
    <properties>
        <phpmavenTagName>${project.version}</phpmavenTagName>
    </properties>

    <parent>
        <groupId>pl.neutrico</groupId>
        <artifactId>java-parent-branches</artifactId>
        <version>1.5</version>
    </parent>

    <groupId>pl.neutrico</groupId>
    <artifactId>generic-java-parent</artifactId>
    <packaging>pom</packaging>

    <name>java-parent:${project.version}</name>

    <description>
        A Maven plugin for dependency management and quality assurance in PHP projects. Uses
        the default Maven lifecycle to provide multiple php features.
    </description>
    
    <scm>
		<connection>scm:git:git://github.com/neutrico/maven-php-plugin.git</connection>
		<developerConnection>scm:git:ssh://git@github.com/neutrico/maven-php-plugin.git</developerConnection>
      <tag>HEAD</tag>
  </scm>

</project>
